# Allow S Pen HAL service to be found
hal_client_domain(system_app, hal_samsung_spen)

# Allow sysfs_kcontroller domain to be adjusted by a system app
allow system_app sysfs_kcontroller:file rw_file_perms;
allow system_app sysfs_battery:file { read };
